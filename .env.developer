# ============================================
# Developer Environment Configuration
# ============================================

# Global Settings
NODE_ENV=development

# Database Configuration
DB_HOST=mock_mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Test@123
DB_NAME=service_dev

# Service Configuration
SERVICE_PORT=3000
JWT_SECRET=abcdefgh
JWT_EXPIRES_IN=24h
CORS_ORIGIN=*

# Host Forward Configuration
HOST_FORWARD_PORT=4000
SERVICE_URL=http://mock_service:3000

# Portal Configuration
PORTAL_PORT=8910
VITE_API_BASE_URL=

# Public Domain (for local development, can use localhost)
PUBLIC_DOMAIN=localhost
PUBLIC_API_PORT=3000
PUBLIC_PORTAL_PORT=8910
PUBLIC_HOST_FORWARD_PORT=80

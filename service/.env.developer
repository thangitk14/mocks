# Environment Configuration for Developer Mode
# Based on docker-compose.yml settings

# Node Environment
NODE_ENV=developer

# Server Configuration
PORT=3000

# Database Configuration
# Note: DB_HOST is set to 'localhost' for local development (not 'mock_mysql' as in docker)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Test@123
DB_NAME=service_dev

# JWT Configuration
JWT_SECRET=abcdefgh
JWT_EXPIRES_IN=24h

# CORS Configuration
CORS_ORIGIN=*

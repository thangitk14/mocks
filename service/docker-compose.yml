version: '3.8'

services:
  # MySQL Database
  mock_mysql:
    image: mysql:8.0
    container_name: mock_service_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: Ttct@835!!
      MYSQL_DATABASE: service_dev
    ports:
      - "3306:3306"
    volumes:
      - mock_mysql_data:/var/lib/mysql
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-pTtct@835!!"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - mock_service_network

  # Node.js Service
  mock_service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mock_service_app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: developer
      DB_HOST: mock_mysql
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: Ttct@835!!
      DB_NAME: service_dev
      JWT_SECRET: your_jwt_secret_key_for_development
      JWT_EXPIRES_IN: 24h
      CORS_ORIGIN: '*'
      PORT: 3000
    depends_on:
      mock_mysql:
        condition: service_healthy
    volumes:
      - ./src:/app/src
      - ./scripts:/app/scripts
      - ./package.json:/app/package.json
      - /app/node_modules
    networks:
      - mock_service_network
    command: npm run dev

volumes:
  mock_mysql_data:
    driver: local

networks:
  mock_service_network:
    driver: bridge
